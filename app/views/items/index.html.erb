<h1>Click a Item To View Content</h1>
<%= link_to 'Create a new item', new_department_item_path(@department) %>
<ul>
  <% @items.each do |aitem| %>
    <li><%= link_to aitem.name, [@department, aitem] %></li>
  <% end %>
</ul>